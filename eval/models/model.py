"""
Abstract model interface for evaluation.

TODO: define a `Model` base class that all model wrappers implement:
  generate(prompt: str, images: list[Path]) -> str
Model hyper-parameters come from the `model:` section of the eval config.
"""
